
README
======

This repo contains plugins for the eduID actions app, the code of which can be
found in `eduid-webapp/src/actions`.

Each plugin resides in `src/eduid_action/<plugin_name>/`, and has its own
README within that directory.

Documentation on the eduID actions app can be found in `eduid-webapp/doc`.
